package com.example.playlistmaker.ui.main.view_model

import androidx.lifecycle.ViewModel

class MainViewModel : ViewModel() {
    // This screen is simple and does not need to interact with Domain or Data layers
}